apiVersion: v1
kind: Service
metadata:
  name: bracu-rag-service
  namespace: default
spec:
  selector:
    app: bracu-rag
  ports:
    - protocol: TCP
      port:  80
      targetPort: 8000
  type: LoadBalancer
